---
"plugin-visual-search-click-target": minor
---

Add optional `image_positions` parameter for specifying custom center positions for each image as percentages of the search area. Record `images` and `image_positions` in trial data output. Images are now center-positioned using the specified coordinates.
